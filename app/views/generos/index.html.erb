<h1>Listando gêneros</h1>

<% if @generos.size.zero? %>
  <br />
  <p> Nenhum gênero cadastrado.</p>
<% else %>
<table>
  <tr>
    <th>Família</th>
    <th>Subfamília</th>
    <th>Gênero</th>
  </tr>

<% @generos.each do |genero| %>
  <tr>
    <td><%=h genero.subfamilia.familia.nome %></td>
    <td><%=h genero.subfamilia.nome %></td>
    <td><%=h genero.nome %></td>
    <td><%= link_to 'Ver', genero %></td>
    <td><%= link_to 'Editar', edit_genero_path(genero) %></td>
    <td><%= link_to 'Excluir', genero, :confirm => 'Você tem certeza?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%end%>
  
<br />

<%= link_to 'Criar gênero', new_genero_path %>
