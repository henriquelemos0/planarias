<h1>Editando município</h1>

<% form_for(@municipio) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </p>
  <p>
    <%= f.label :estado_id %><br />
    <%= @estados = Estado.find(:all, :order => "pais.nome", :order => "nome")
        f.collection_select(:estado_id, @estados, :id, :nome) %>
  </p>
  <p>
    <%= f.submit 'Atualizar' %>
  </p>
<% end %>

<%= link_to 'Ver', @municipio %> |
<%= link_to 'Voltar', municipios_path %>